<%= form_with(model: boca, local: true) do |form| %>
  <% if boca.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(boca.errors.count, "error") %> prohibited this boca from being saved:</h2>

      <ul>
        <% boca.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :cliente_id %>
    <%= form.text_field :cliente_id %>
  </div>

  <div class="field">
    <%= form.label :servico_id %>
    <%= form.text_field :servico_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
