<%= form_with(model: dmarcacao, local: true) do |form| %>
  <% if dmarcacao.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(dmarcacao.errors.count, "error") %> prohibited this dmarcacao from being saved:</h2>

      <ul>
        <% dmarcacao.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :cliente_id %>
    <%= form.text_field :cliente_id %>
  </div>

  <div class="field">
    <%= form.label :start %>
    <%= form.datetime_select :start %>
  </div>

  <div class="field">
    <%= form.label :end %>
    <%= form.datetime_select :end %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
