<%= form_with(model: doc_categorium, local: true) do |form| %>
  <% if doc_categorium.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(doc_categorium.errors.count, "error") %> prohibited this doc_categorium from being saved:</h2>

      <ul>
        <% doc_categorium.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nome %>
    <%= form.text_field :nome %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
